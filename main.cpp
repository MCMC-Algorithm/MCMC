#include <iostream>
#include <random>
#include <vector>
#include <ctime>
using namespace std;

int initialState;
int edgeProbability;
vector<vector<int>> transitionMatrix(0);
vector<vector<int>> transitionMatrix(0);
vector<vector<vector<int>>> probabilityMatrix;
int hours;
int timeeach;
int msize;
